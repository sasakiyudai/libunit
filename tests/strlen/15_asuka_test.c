#include "basic_tests.h"
#include <string.h>
#include "../../real-tests/libft.h"

int	asuka_test(void)
{
	if (ft_strlen("anta bakala?") == strlen("anta bakala?"))
		return (0);
	else
		return (-1);
}
