#include "basic_tests.h"
#include <string.h>
#include "../../libft/libft.h"
int bus_str_test(void)
{
	char *literal = "literal";

	literal[0] = 'L';
if (ft_strlen(" ") == strlen(""))
return(0);
else
return(-1);
}